package com.company.view;

/**
 * A view that renders the animation to the screen.
 */
public interface VisualView {
  /**
   * Renders the animation graphically to the screen using Swing.
   */
  void renderVisual();
}
